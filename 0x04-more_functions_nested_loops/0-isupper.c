#include "main.h"

/**
 * _isupper - checks if a character is uppercare or not
 * @c: character to be tested
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
	return ((c >= 'A') && (c <= 'Z'));
}
